<% provide(:title, t("users.edit_title")) %>
<% provide(:button_text, t("users.save_button")) %>
<h1><%= t "users.update_line"%></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render "form" %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <%= link_to (raw t("change")), t("link_pages.gravatar") %>
    </div>
  </div>
</div>
